<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.5.16</version>
        <relativePath/>
    </parent>
    <groupId>com.wcdk.process</groupId>
    <artifactId>wcdk-process</artifactId>
    <name>wcdk-process</name>
    <description>WCDK-流程引擎</description>
    <properties>
        <java.version>21</java.version>
    </properties>
    <version>1.0.0</version>

    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>com.wcdk.r2dbc</groupId>
            <artifactId>wcdk-r2dbc</artifactId>
            <version>3.5.16</version>
        </dependency>
        <dependency>
            <groupId>dm.r2dbc</groupId>
            <artifactId>dm-r2dbc</artifactId>
            <version>1.0.0</version>
        </dependency>
        <dependency>
            <groupId>com.dameng</groupId>
            <artifactId>dm-jdbc-driver</artifactId>
            <version>8.1.3.73</version>
        </dependency>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <version>1.18.38</version>
        </dependency>
        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
            <version>2.8.13</version>
        </dependency>
        <dependency>
            <groupId>com.github.xiaoymin</groupId>
            <artifactId>knife4j-openapi3-ui</artifactId>
            <version>4.5.0</version>
        </dependency>
    </dependencies>

    <profiles>
        <!-- 达梦数据库 -->
        <profile>
            <id>dm</id>
            <activation>
                <activeByDefault>true</activeByDefault>
            </activation>
        </profile>

        <!-- PostgreSQL数据库 -->
        <profile>
            <id>postgres</id>
        </profile>

        <!-- MySQL数据库 -->
        <profile>
            <id>mysql</id>
        </profile>

        <!-- Oracle数据库 -->
        <profile>
            <id>oracle</id>
        </profile>

        <!-- 所有数据库驱动（开发环境使用） -->
        <profile>
            <id>all</id>
        </profile>
    </profiles>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.14.1</version>
                <configuration>
                    <source>21</source>
                    <target>21</target>
                    <release>21</release>
                    <encoding>UTF-8</encoding>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>
</project>
